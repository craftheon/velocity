{
  "name": "hexagon-warrior",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "e2e/*"
  ],
  "scripts": {
    "dev": "echo \"Error: no test specified\" && exit 1"
  }
}
